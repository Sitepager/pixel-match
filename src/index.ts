// Main entry point: re-export browser version by default
export * from './browser';
// For Node.js, use src/node/index.ts as the entry point
// For browser, use src/browser/index.ts as the entry point
